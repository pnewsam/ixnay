import { Section } from "../ui/Section";

export const SelectPage = () => {
  return (
    <Section>
      <h1>Select</h1>
      <p>
        A select dropdown. Similar to the native browser select, but with better
        styling.
      </p>
      <hr />
    </Section>
  );
};
